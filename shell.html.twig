{#
===============================================
	GRID
===============================================
#}

{#
	Open container.
#}
{% macro container_open(element, id, extra_classes, properties) %}
	<{{ element|default('div') }} class="{{ 'container'|apply_filters( 'maera/container_class' ) }}{{ extra_classes ? ' '~extra_classes : '' }}"{{ id ? ' id="'~id~'"' : '' }}{{ properties ? ' '~properties : '' }}>
{% endmacro %}


{#
	Close container.
#}
{% macro container_close(element) %}
	</{{ element|default('div') }}>
{% endmacro %}


{#
	Open row.
#}
{% macro row_open(element, id, extra_classes, properties) %}
	<{{ element|default('div') }} class="row{{ extra_classes ? ' '~extra_classes : '' }}"{{ id ? ' id="'~id~'"' : '' }}{{ properties ? ' '~properties : '' }}>
{% endmacro %}


{#
	Close row
#}
{% macro row_close(element) %}
	</{{ element|default('div') }}>
{% endmacro %}


{#
	Open column
#}
{% macro column_open(element, sizes, id, extra_classes, properties) %}
	<{{ element|default('div') }} class="{{ _self.column_classes( sizes ) }}{{ extra_classes ? ' '~extra_classes : '' }}"{{ id ? ' id="'~id~'"' : '' }}{{ properties ? ' '~properties : '' }}>
{% endmacro %}


{#
	Close column
#}
{% macro column_close(element) %}
	</{{ element|default('div') }}>
{% endmacro %}


{#
	Calculate column classes
#}
{% macro column_classes(sizes) %}
	{% if sizes is iterable %}
		{% for size in sizes %}
			{% if size.mobile %} col-xs-{{ size.mobile }} {% endif %}
			{% if size.tablet %} col-sm-{{ size.tablet }} {% endif %}
			{% if size.medium %} col-md-{{ size.medium }} {% endif %}
			{% if size.large %} col-lg-{{ size.large }} {% endif %}
		{% endfor %}
	{% endif %}
{% endmacro %}

{#
===============================================
	BUTTONS
===============================================
#}

{#
	Make dropdown buttons
#}
{% macro button_dropdown(color, size, type, extra, label, content) %}

	<div class="btn-group">
		<button type="button" class="dropdown-toggle {{ _self.button_classes(color, size, type) }}" data-toggle="dropdown">
			{{ label }} <span class="caret"></span>
		</button>

		<ul class="dropdown-menu" role="menu">{{ content }}</ul>
	</div>

{% endmacro %}

{#
	Calculate button classes
#}
{% macro button_classes(color, size, type, extra) %}

	btn
	btn-{{ color }}

	{% if size == 'extra-small' %}btn-xs{% endif %}
	{% if size == 'small' %}btn-sm{% endif %}
	{% if size == 'medium' %}{# No need for an extra class #}{% endif %}
	{% if size == 'large' %}btn-lg{% endif %}
	{% if size == 'extra-large' %}btn-lg{% endif %}

	{{ type }}
	{{ extra }}

{% endmacro %}


{#
	Calculate button group classes
#}
{% macro button_group_classes(size, type, extra_classes) %}

	btn-group
	{% if size == 'extra-small' %}btn-group-xs{% endif %}
	{% if size == 'small' %}btn-group-sm{% endif %}
	{% if size == 'medium' %}{# No need for an extra class #}{% endif %}
	{% if size == 'large' %}btn-group-lg{% endif %}
	{% if size == 'extra-large' %}btn-group-lg{% endif %}
	{{ type }}
	{{ extra_classes }}

{% endmacro %}

{#
===============================================
	ALERTS
===============================================
#}

{% macro alert(type, message, id, class, close) %}
	<div class="alert{{ type ? ' alert-'~type : '' }} fade in {{ class|default('') }} {% if close|default(false) %}alert-dismissable{% endif %}">
		{% if close|default(false) %}
			<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
		{% endif %}
		{{ message|raw }}
	</div>
{% endmacro %}

{#
===============================================
	PANELS
===============================================
#}

{#
	Open panel
#}
{% macro panel_open(extra_classes, id) %}
	<div class="panel{{ extra_classes ? ' '~extra_classes : '' }}"{{ id ? ' id="'~id~'"' : '' }}>
{% endmacro %}

{#
	Close panel
#}
{% macro panel_close() %}
	</div>
{% endmacro %}

{#
	Open panel heading
#}
{% macro panel_heading_open(extra_classes) %}
	<div class="panel-heading{{ extra_classes ? ' '~extra_classes : '' }}">
{% endmacro %}

{#
	Close panel heading
#}
{% macro panel_heading_close() %}
	</div>
{% endmacro %}

{#
	Open panel body
#}
{% macro panel_body_open(extra_classes) %}
	<div class="panel-body{{ extra_classes ? ' '~extra_classes : '' }}">
{% endmacro %}

{#
	Close panel body
#}
{% macro panel_body_close() %}
	</div>
{% endmacro %}

{#
	Open panel footer
#}
{% macro panel_footer_open(extra_classes) %}
	<div class="panel-body{{ extra_classes ? ' '~extra_classes : '' }}">
{% endmacro %}

{#
	Close panel footer
#}
{% macro panel_footer_close() %}
	</div>
{% endmacro %}

{#
===============================================
	UTILITIES
===============================================
#}

{#
	Clearfix
#}
{% macro clearfix() %}
	<div class="clearfix"></div>
{% endmacro %}

{#
	Pagination <ul> class
#}
{% macro pagination_ul_class() %}
	pagination
{% endmacro %}

{#
	Float classes
#}
{% macro float_class(alignment) %}
	pull-{{ alignment }}
{% endmacro %}

{#
	Icons
#}
{% macro icon(symbol) %}
	<i class="el-icon-{{ symbol }}"></i>
{% endmacro %}
